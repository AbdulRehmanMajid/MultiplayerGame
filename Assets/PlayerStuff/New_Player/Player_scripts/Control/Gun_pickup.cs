using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gun_pickup : MonoBehaviour
{
    public gunMangerV2 gunMangerV2;
  
    // Start is called before the first frame update
   

    // Update is called once per frame
    
}
